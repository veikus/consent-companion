{
  "name": "consent-companion",
  "scripts": {
    "start": "parcel watch manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build manifest.json --config @parcel/config-webextension --no-optimize"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.9.3",
    "parcel": "^2.9.3"
  }
}
